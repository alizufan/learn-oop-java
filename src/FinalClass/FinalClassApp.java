package FinalClass;

// # Final Class
//   - Previously we used the final keyword in Java.
//   - If used in a variable, the variable's data cannot change any more.
//   - Final can also be used in classes, where if we use the final keyword before the class, then we indicate that the class cannot be inherited anymore.
//   - Automatically all child classes will error.
//
public class FinalClassApp {
  public static void main(String[] args) {}
}
